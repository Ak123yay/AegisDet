# Configs

Store one machine-readable configuration per reproducible run family. Configs should include model, data, image size, training/inference settings, module parameters, seed, device, precision, output identity, and phase. Copy the resolved config into every run artifact; never rely on undocumented CLI overrides.
