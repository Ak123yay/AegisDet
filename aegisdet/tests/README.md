# Tests

The deterministic test suite covers geometry, padding, remapping, rule routing, crop prioritization, class-aware merge, EdgeScore, and percentile calculations. Run `PYTHONPATH=src python -m unittest discover -s tests -v` before benchmark or release work.
