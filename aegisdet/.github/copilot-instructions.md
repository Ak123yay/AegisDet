Follow `/AGENTS.md`.

Before code changes, read the sibling vault's `TASKS.md`, `CURRENT_STATE.md`, `CONTEXT_LOCK.md`, and `wiki/_code-map.md`.

After changes, use `python tools/workspace_sync.py record` to update task evidence and vault state. Never fabricate completion or metrics.
