#!/usr/bin/env sh
set -eu
if command -v python >/dev/null 2>&1; then
  python tools/workspace_sync.py pre-commit
elif command -v py >/dev/null 2>&1; then
  py -3 tools/workspace_sync.py pre-commit
else
  echo "AegisDet hook: Python was not found." >&2
  exit 1
fi
