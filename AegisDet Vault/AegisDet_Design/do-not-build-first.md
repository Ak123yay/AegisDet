# Do Not Build First

Blocked until the corresponding phase:

- custom global-token neural block — Phase 4;
- learned router — Phase 5;
- multi-teacher distillation — Phase 6;
- QAT — Phase 7 and only after PTQ loss;
- ByteTrack — after image-level system stability;
- custom detection head — only after a measured head bottleneck;
- neural architecture search — outside the core project;
- multi-domain detector — after the first complete research cycle.

The next task is environment verification, not architecture invention.
