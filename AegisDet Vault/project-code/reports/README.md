# Reports

Scripts write structured JSON/CSV summaries and plots here during local development. Each report must be traceable to a run, model artifact, dataset version, commit, hardware, runtime, precision, and command. Final copies or summaries may be promoted to the vault `output/` directory.
