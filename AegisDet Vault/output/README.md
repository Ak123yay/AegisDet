# Output

Generated evidence belongs here: environment checks, audit reports, CSV/JSON metrics, plots, model-comparison tables, and exported summaries. Keep large weights and datasets outside the vault or Git repository. Every output should identify the originating experiment, config, dataset version, commit, device, and runtime.
